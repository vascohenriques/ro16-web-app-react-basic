import React from "react";
import ViewWrapper from "../../shared/components/ViewWrapper";

const Meetings = (props) => {
  return <ViewWrapper titleText={"Meetings"}></ViewWrapper>;
};

export default Meetings;
